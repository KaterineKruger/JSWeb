// CÓDIGO NÃO EXECUTÁVEL

// ---Procedural
processamento (valor01, valor02, valor03)
// *Processamento manipulando dados (sejam globais ou internos da função), retornando um valor

// ---OO
objeto = {
  valor01,
  valor02,
  valor03
  processamento() {
    // ...
  }
}

objeto.processamento()
/* *Objeto que tem valores (atributos) e funções (comportamentos). Você passa a invocar esses
processamentos (funções) a partir do dado (atributo). -O foco passou a ser o objeto. */

// Princípios importantes:
// 1. Abstração;
// 2. Encapsulamento;
// 3. Herança (prototype);
// 4. Polimorfismo.